---
domain: mongo_cps
descriptors:
  - key: database
    value: users
    rate_limit:
      unit: day
      requests_per_unit: 5
    descriptors:
      - key: user_id
        rate_limit:
          unit: day
          requests_per_unit: 100
      - key: user_id
        value: 3
        rate_limit:
          unit: day
          requests_per_unit: 2

  - key: database
    value: default
    rate_limit:
      unit: day
      requests_per_unit: 5

  - key: database
    rate_limit:
      unit: day
      requests_per_unit: 10

  - key: foo
    value: bar
    rate_limit:
      unit: day
      requests_per_unit: 10
