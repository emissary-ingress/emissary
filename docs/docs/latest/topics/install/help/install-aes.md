# edgectl install: Can't apply the AES manifests
 
Ambassador's `edgectl install` uses `kubectl` to communicate with Kubernetes.  

Ambassador was unable to perform `edgectl apply` on the AES manifests.
