---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: qpr-regex-target
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qpr-regex-target
  template:
    metadata:
      labels:
        app: qpr-regex-target
    spec:
      containers:
        - name: kat-server
          image: (env('KAT_SERVER_IMAGE'))
          imagePullPolicy: IfNotPresent
          command: ["/work/kat-server"]
          env:
            - name: BACKEND
              value: qpr-regex-target
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: qpr-regex-target
spec:
  selector:
    app: qpr-regex-target
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
---
apiVersion: getambassador.io/v3alpha1
kind: Mapping
metadata:
  name: queryparameter-routing-regex-target
spec:
  ambassador_id: [(env('SLOT'))]
  hostname: "*"
  prefix: /queryparameter-routing-regex/
  service: qpr-regex-target:8080
  regex_query_parameters:
    test_param: "^[a-z].*"
