---
# Port of KAT's tests/kat/t_gzip.py::GzipTest.
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  labels:
    flavor: slot2
  name: gzip-content-type
spec:
  bindings:
    - name: kat_server_image
      value: (env('KAT_SERVER_IMAGE'))
    - name: kat_client
      value: (env('KAT_CLIENT'))
    - name: gateway_url
      value: (env('GATEWAY_URL'))
  steps:
    - name: apply-and-probe
      try:
        - apply:
            file: manifests.yaml
            template: true
        - script:
            timeout: 2m
            env:
              - name: KAT_CLIENT
                value: ($kat_client)
              - name: GATEWAY_URL
                value: ($gateway_url)
            content: |
              ../../probe.sh queries.json '
                .[0].result.status == 200 and
                .[0].result.headers["Content-Encoding"][0] == "gzip"
              '
      catch:
        - describe:
            apiVersion: v1
            kind: Pod
        - podLogs:
            selector: app=gzip-ct-echo
            tail: 200
        - get:
            apiVersion: getambassador.io/v3alpha1
            kind: Module
        - get:
            apiVersion: getambassador.io/v3alpha1
            kind: Mapping
        - script:
            content: |
              kubectl -n emissary-slot2 logs -l app.kubernetes.io/name=emissary-ingress --tail=200 || true
