---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: request-header-preserve-id-echo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: request-header-preserve-id-echo
  template:
    metadata:
      labels:
        app: request-header-preserve-id-echo
    spec:
      containers:
        - name: kat-server
          image: (env('KAT_SERVER_IMAGE'))
          imagePullPolicy: IfNotPresent
          command: ["/work/kat-server"]
          env:
            - name: BACKEND
              value: request-header-preserve-id-echo
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: request-header-preserve-id-echo
spec:
  selector:
    app: request-header-preserve-id-echo
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
---
# The Module is global to one Emissary instance, hence `slot: exclusive`.
apiVersion: getambassador.io/v3alpha1
kind: Module
metadata:
  name: ambassador
spec:
  ambassador_id: [(env('SLOT'))]
  config:
    preserve_external_request_id: true
---
apiVersion: getambassador.io/v3alpha1
kind: Mapping
metadata:
  name: target
spec:
  ambassador_id: [(env('SLOT'))]
  hostname: "*"
  prefix: /target/
  service: request-header-preserve-id-echo:8080
