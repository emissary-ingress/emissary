---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: max-request-header-kb-max-echo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: max-request-header-kb-max-echo
  template:
    metadata:
      labels:
        app: max-request-header-kb-max-echo
    spec:
      containers:
        - name: kat-server
          image: (env('KAT_SERVER_IMAGE'))
          imagePullPolicy: IfNotPresent
          command: ["/work/kat-server"]
          env:
            - name: BACKEND
              value: max-request-header-kb-max-echo
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: max-request-header-kb-max-echo
spec:
  selector:
    app: max-request-header-kb-max-echo
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
---
# The Module is global to one Emissary instance, hence `slot: exclusive`.
apiVersion: getambassador.io/v3alpha1
kind: Module
metadata:
  name: ambassador
spec:
  ambassador_id: [(env('SLOT'))]
  config:
    max_request_headers_kb: 96
---
apiVersion: getambassador.io/v3alpha1
kind: Mapping
metadata:
  name: target
spec:
  ambassador_id: [(env('SLOT'))]
  hostname: "*"
  prefix: /target/
  service: max-request-header-kb-max-echo:8080
