---
# Port of KAT's tests/kat/t_basics.py::ServerNameTest.
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  labels:
    slot: exclusive
  name: server-name
spec:
  steps:
    - name: apply-and-probe
      try:
        - apply:
            file: manifests.yaml
        - script:
            timeout: 2m
            content: |
              ../../probe.sh queries.json '
                .[0].result.status == 301 and
                .[0].result.headers["Server"][0] == "test-server"
              '
