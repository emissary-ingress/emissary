---
apiVersion: v1
kind: Service
metadata:
  name: rl-test
  annotations:
    getambassador.io/config: |
      ---
      apiVersion: ambassador/v0
      kind: Mapping
      name: rl-test
      prefix: /rl/
      service: httpbin.org
      rate_limits:
      - descriptor: rldesc
spec:
  ports:
  - port: 80
