group: travis_latest
language: python
env:
    - REPO=datawire/ambassador  # has .travis.yml file
    - REPO=datawire/ambassador-auth-httpbasic  # has .travis.yml file
    - REPO=datawire/ambassador-auth-httpjwt  # has .travis.yml file
    - REPO=datawire/ambassador-canary
    - REPO=datawire/bakerstreet
    - REPO=datawire/boston-devops-days-2017  # has .travis.yml file
    - REPO=datawire/docker-shim
    - REPO=datawire/envoy-canary
    - REPO=datawire/envoy-steps
    - REPO=datawire/forge  # has .travis.yml file
    - REPO=datawire/gateway
    - REPO=datawire/ghas
    - REPO=datawire/hello-forge
    - REPO=datawire/hello-forge-network
    - REPO=datawire/hello-kubernetes
    - REPO=datawire/hello-webapp
    - REPO=datawire/hello-world
    - REPO=datawire/hello-world-python
    - REPO=datawire/kubeflow  # has .travis.yml file
    - REPO=datawire/kubernaut  # has .travis.yml file
    - REPO=datawire/kubernaut-agent  # has .travis.yml file
    - REPO=datawire/micromaze
    - REPO=datawire/qotm  # has .travis.yml file
    - REPO=datawire/qotm-ws  # has .travis.yml file
    - REPO=datawire/reference-infrastructure-fabric  # has .travis.yml file
    - REPO=datawire/scout.py  # has .travis.yml file
    - REPO=datawire/scout2csv  # has .travis.yml file
    - REPO=datawire/sscid
    - REPO=datawire/sshuttle  # has .travis.yml file
    - REPO=datawire/toybox
cache: pip
python:
    - 2.7
    - 3.6
    #- nightly
    #- pypy
    #- pypy3
matrix:
    allow_failures:
        - python: 2.7
        - python: nightly
        - python: pypy
        - python: pypy3
install:
    #- pip install -r requirements.txt
    - pip install flake8  # pytest  # add other testing frameworks later
before_script:
    - URL=https://github.com/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
    - cd ~/${REPO}
script:
    - echo stop the build if there are Python syntax errors or undefined names
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    #- true  # pytest --capture=sys
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
